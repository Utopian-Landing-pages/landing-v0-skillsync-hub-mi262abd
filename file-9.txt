'use client'

import { motion } from 'framer-motion'
import { Check } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { PROJECT_URL } from '@/lib/constants'

const pricingTiers = [
  {
    name: 'Client Base',
    price: '10-15%',
    period: '/per project',
    description: 'Perfect for occasional project needs',
    features: [
      'Standard matching',
      'Secure payments',
      'Project management tools',
      'Basic support'
    ],
    cta: 'Post a Project',
    popular: false
  },
  {
    name: 'Client Premium',
    price: '$199-$499',
    period: '/month',
    description: 'For businesses with ongoing talent needs',
    features: [
      'Advanced analytics',
      'Dedicated account manager',
      'Priority matching',
      'Enhanced talent pool access',
      'Custom reporting',
      'API access'
    ],
    cta: 'Upgrade to Premium',
    popular: true
  },
  {
    name: 'Freelancer Pro',
    price: '$29',
    period: '/month',
    description: 'Boost your freelance career',
    features: [
      'Enhanced profile visibility',
      'Performance insights',
      'Personalized project recommendations',
      'Early access to listings',
      'Priority support'
    ],
    cta: 'Become a Pro',
    popular: false
  }
]

export function Pricing() {
  return (
    <section id="pricing" className="py-20 sm:py-28 bg-background">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          viewport={{ once: true }}
          transition={{ duration: 0.6 }}
          className="text-center mb-16"
        >
          <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-foreground mb-4 text-balance">
            Choose Your <span className="text-primary">Plan</span>
          </h2>
          <p className="text-lg text-muted-foreground max-w-2xl mx-auto text-balance">
            Flexible pricing for freelancers and businesses of all sizes
          </p>
        </motion.div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
          {pricingTiers.map((tier, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 0.5, delay: index * 0.1 }}
              className={`relative bg-card border ${
                tier.popular
                  ? 'border-primary shadow-lg shadow-primary/20'
                  : 'border-border'
              } rounded-2xl p-8 hover:border-primary/50 transition-all duration-300`}
            >
              {tier.popular && (
                <div className="absolute -top-4 left-1/2 -translate-x-1/2">
                  <span className="bg-primary text-primary-foreground px-4 py-1 rounded-full text-sm font-medium">
                    Most Popular
                  </span>
                </div>
              )}

              <div className="text-center mb-6">
                <h3 className="text-2xl font-bold text-foreground mb-2">{tier.name}</h3>
                <p className="text-muted-foreground text-sm mb-4">{tier.description}</p>
                <div className="flex items-baseline justify-center gap-1">
                  <span className="text-4xl font-bold text-foreground">{tier.price}</span>
                  <span className="text-muted-foreground">{tier.period}</span>
                </div>
              </div>

              <ul className="space-y-3 mb-8">
                {tier.features.map((feature, featureIndex) => (
                  <li key={featureIndex} className="flex items-start gap-3">
                    <div className="w-5 h-5 rounded-full bg-primary/10 flex items-center justify-center flex-shrink-0 mt-0.5">
                      <Check className="w-3 h-3 text-primary" />
                    </div>
                    <span className="text-muted-foreground text-sm">{feature}</span>
                  </li>
                ))}
              </ul>

              <Button
                className={`w-full ${
                  tier.popular
                    ? 'bg-primary hover:bg-primary/90 text-primary-foreground'
                    : 'bg-secondary hover:bg-secondary/80 text-secondary-foreground'
                }`}
                asChild
              >
                <a href={PROJECT_URL} target="_blank" rel="noopener noreferrer">
                  {tier.cta}
                </a>
              </Button>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  )
}
